services:
    mustache:
        image: python
        volumes:
            - ./src:/app/src
            - ./build:/app/build
            - ./scripts:/app/scripts
        working_dir: /app
        entrypoint: ["sh", "-c", "pip install  pyinotify && ./scripts/build.py src && ./scripts/build.py --watch --output test src"]
