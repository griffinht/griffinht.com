<html>
<head>
    {{> ../../head}}
    <style>
article {
    background-color: var(--neutral-background);
    padding: 1em;
    margin: 1em auto;
    max-width: 60em;
}

img {
    width: 100%;
}

    </style>
</head>
<body>
    <article>
        <h1>{{title}}</h1>
        <time datetime="{{datetime}}">{{datetime}}</time>
        (<a href="{{> ../../source}}">source</a>)
        {{{content}}}
    </article>
</body>
</html>
